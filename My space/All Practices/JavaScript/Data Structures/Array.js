const arr = [1, 2, 3, "good"]

arr.push(4) /* to add an element at the end of an array */

arr.unshift(0) /* to add an element at the beginning of an array */

arr.pop() /* to remove an element at the end of an array  */

arr.shift() /* to remove an element at the beginning of an array */

// most important  methods
// map , filter, reduce , concat , slice and splice

// console.log(arr)
for (let item of arr) {
	console.log(item)
}
